# 版本管理后台 (Vue Admin)

多应用版本管理和广告配置后台管理系统。

## 📦 技术栈

- Vue 3 (Composition API)
- Vite 7.x
- Element Plus 2.x
- Vue Router 4.x
- Axios

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问：`http://localhost:5173`

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

---

## 🔑 登录信息

**用户名**：`admin`
**密码**：`123456`

> 注意：登录逻辑在 `src/views/Login.vue`，可根据需要修改

---

## 🎯 功能模块

### 1️⃣ 版本管理

**路径**：`/version-manager`

**功能**：
- ✅ 多应用版本管理（Tab切换）
- ✅ 动态添加/删除应用
- ✅ 内联编辑版本信息
- ✅ 下载方式配置（direct/web）
- ✅ 实时API文档预览
- ✅ 一键保存所有配置

**字段说明**：
- **应用ID**：唯一标识符（小写字母+下划线）
- **版本号**：如 1.0.0
- **版本Code**：数字，用于比较（如 100）
- **下载方式**：
  - `应用内直接下载`：App内直接下载APK
  - `跳转浏览器下载`：打开浏览器下载
- **下载地址**：APK下载链接或网页链接
- **是否强制更新**：开启后App必须更新才能使用
- **更新说明**：显示给用户的更新内容

**操作指南**：
1. 点击 `+` 图标添加新应用
2. 输入应用英文ID（如：myapp）
3. 编辑版本信息
4. 点击"保存上线"按钮

---

### 2️⃣ 广告配置

**路径**：`/ad-manager`

**功能**：
- ✅ 多广告位管理（Tab切换）
- ✅ 动态添加/删除广告位
- ✅ 支持多张广告配置
- ✅ 实时手机预览效果
- ✅ 上线/下线状态控制

**预设广告位**：
- `splash`：开屏广告
- `home_banner`：首页轮播
- `popup`：首页弹窗
- 支持自定义广告位

**字段说明**：
- **标题**：后台备注用
- **图片URL**：广告图片地址
- **跳转URL**：点击后跳转链接
- **状态**：上线(1) / 下线(0)
- **ID**：自动生成，唯一标识

**操作指南**：
1. 选择广告位Tab
2. 点击"添加一个新广告"
3. 填写图片URL和跳转链接
4. 设置状态为"上线"
5. 点击"保存上线"

---

## 📂 目录结构

```
vue_app/
├── public/                   # 静态资源
│   └── config.json           # API配置文件
├── src/
│   ├── main.js               # 入口文件
│   ├── App.vue               # 根组件
│   ├── router/               # 路由配置
│   │   └── index.js          # 路由定义
│   ├── views/                # 页面组件
│   │   ├── Login.vue         # 登录页
│   │   ├── Home.vue          # 主页布局
│   │   ├── VersionManager.vue  # 版本管理页（新）
│   │   └── AdManager.vue     # 广告配置页
│   ├── components/           # 公共组件
│   │   └── VersionForm.vue   # 版本表单（已废弃）
│   ├── utils/                # 工具函数
│   │   └── config.js         # 配置读取
│   └── store/                # 状态管理
├── index.html                # HTML模板
├── vite.config.js            # Vite配置
├── package.json              # 依赖配置
└── README.md                 # 本文档
```

---

## ⚙️ 配置说明

### API 基础地址配置

**文件位置**：`public/config.json`

```json
{
  "apiBaseUrl": "http://localhost:3004"
}
```

**修改步骤**：
1. 编辑 `public/config.json`
2. 修改 `apiBaseUrl` 为你的后端地址
3. 重启前端服务

**为什么用配置文件？**
- 打包后可直接修改，无需重新编译
- 方便部署到不同环境

---

## 🔌 API 接口调用

所有API调用都通过 `axios` + `getConfig()` 实现：

```javascript
import axios from 'axios'
import { getConfig } from '../utils/config'

// 获取配置
const config = await getConfig()
const apiBase = config.apiBaseUrl

// 调用API
const res = await axios.get(`${apiBase}/api/version/manage/list`)
```

### 接口列表

#### 版本管理接口

| 接口 | 方法 | 用途 |
|------|------|------|
| `/api/version/manage/list` | GET | 获取所有应用版本 |
| `/api/version/manage/save` | POST | 保存所有应用版本 |

#### 广告管理接口

| 接口 | 方法 | 用途 |
|------|------|------|
| `/api/ads/manage/list` | GET | 获取所有广告配置 |
| `/api/ads/manage/save` | POST | 保存所有广告配置 |

---

## 🎨 页面结构

### 版本管理页面布局

```
┌─────────────────────────────────────────────────────┐
│  📱 应用版本管理    [新建应用] [重置] [保存上线]    │
├─────────────────────────────────────┬───────────────┤
│  Tab: [懒人连点器] [LM播放器] [+]  │               │
│                                     │  📖 API文档   │
│  ┌───────────────────────────────┐ │               │
│  │ 应用ID:    clickmate          │ │  GET /api/    │
│  │ 版本号:    1.0.0              │ │  version?     │
│  │ 版本Code:  1                  │ │  appId=...    │
│  │ 下载方式:  ◉直接下载 ○浏览器  │ │               │
│  │ 下载地址:  https://...        │ │  返回示例:    │
│  │ 强制更新:  ⚪                  │ │  {...}        │
│  │ 更新说明:  [文本框]           │ │               │
│  └───────────────────────────────┘ │               │
│                                     │               │
└─────────────────────────────────────┴───────────────┘
```

### 广告管理页面布局

```
┌─────────────────────────────────────────────────────┐
│  📺 广告配置       [新建广告位] [重置] [保存上线]   │
├─────────────────────────────────────┬───────────────┤
│  Tab: [开屏广告] [首页轮播] [+]    │               │
│                                     │   📱 手机     │
│  ┌ 广告1 ────────────────────────┐ │   预览        │
│  │ 上线中  | 开屏广告示例         │ │               │
│  │ ▼                              │ │  ┌─────────┐ │
│  │  标题: 开屏广告                │ │  │         │ │
│  │  图片: https://...             │ │  │  [图片]  │ │
│  │  链接: https://...             │ │  │         │ │
│  │  状态: ◉上线 ○下线             │ │  └─────────┘ │
│  └────────────────────────────────┘ │               │
│  [+ 添加一个新广告]                 │               │
└─────────────────────────────────────┴───────────────┘
```

---

## 🔐 路由守卫

### 登录状态检查

```javascript
// src/router/index.js
router.beforeEach((to, from, next) => {
  const isLoggedIn = localStorage.getItem('isLoggedIn') === 'true'

  if (to.path === '/login') {
    // 已登录，跳转首页
    if (isLoggedIn) {
      next('/')
    } else {
      next()
    }
  } else {
    // 未登录，跳转登录页
    if (isLoggedIn) {
      next()
    } else {
      next('/login')
    }
  }
})
```

### 退出登录

点击右上角"退出登录"按钮：
1. 清除 localStorage 中的 `isLoggedIn`
2. 跳转到登录页

---

## 🎯 核心功能实现

### 版本管理 - 多Tab设计

**参考 AdManager 的成熟设计**：
```vue
<el-tabs
  v-model="activeApp"
  type="card"
  editable
  @edit="handleTabEdit"
>
  <el-tab-pane
    v-for="(versionData, appId) in versionConfig"
    :key="appId"
    :label="formatAppName(appId)"
    :name="appId"
  >
    <!-- 编辑表单 -->
  </el-tab-pane>
</el-tabs>
```

**关键特性**：
- `editable`：可编辑Tab（显示 + 和 × 按钮）
- `@edit`：监听Tab的添加/删除事件
- `v-for`：动态渲染所有应用

---

### 广告管理 - 实时预览

**手机模拟器预览**：
```vue
<div class="phone-mockup">
  <div class="notch"></div>  <!-- 刘海屏 -->
  <div class="screen">
    <!-- 根据当前Tab显示不同场景 -->
    <div v-if="activePosition === 'splash'">
      <!-- 开屏广告预览 -->
    </div>
    <div v-else-if="activePosition === 'home_banner'">
      <!-- 首页轮播预览 -->
    </div>
  </div>
  <div class="home-indicator"></div>  <!-- 底部横条 -->
</div>
```

**只预览上线广告**：
```javascript
const currentActiveAds = computed(() => {
  const ads = adData.value[activePosition.value] || []
  return ads.filter(ad => ad.status === 1 && ad.imageUrl)
})
```

---

## 🐛 常见问题

### Q1: 启动报错 `command not found: vite`

**原因**：依赖未安装

**解决**：
```bash
npm install
npm run dev
```

---

### Q2: API 请求 404 或 CORS 错误

**原因**：后端服务未启动或地址配置错误

**解决**：
1. 确保后端服务已启动：`http://localhost:3004`
2. 检查 `public/config.json` 中的 `apiBaseUrl`
3. 查看浏览器控制台的错误信息

---

### Q3: 登录后跳转失败

**原因**：路由配置问题

**解决**：
```javascript
// 确保 localStorage 存储成功
localStorage.setItem('isLoggedIn', 'true')

// 使用 replace 而不是 push
router.replace('/')
```

---

### Q4: Element Plus 图标不显示

**原因**：图标库未正确导入

**解决**：
```bash
npm install @element-plus/icons-vue
```

```vue
<!-- 在组件中导入 -->
<script setup>
import { Plus, Refresh, Check } from '@element-plus/icons-vue'
</script>
```

---

### Q5: 如何修改登录密码？

编辑 `src/views/Login.vue`：
```javascript
function handleLogin() {
  if (form.username === 'admin' && form.password === '你的新密码') {
    // 登录成功
  }
}
```

---

## 📊 数据流程

### 版本管理数据流

```
用户操作 → VersionManager.vue
    ↓
fetchVersions()
    ↓
GET /api/version/manage/list
    ↓
versionConfig.value = res.data
    ↓
用户编辑
    ↓
saveVersions()
    ↓
POST /api/version/manage/save
    ↓
后端保存到 versions.json
```

---

## 🚀 部署指南

### 开发环境

```bash
npm run dev
```

### 生产环境

**1. 构建**：
```bash
npm run build
```

**2. 部署**：
将 `dist/` 目录部署到静态服务器：
- Nginx
- Apache
- Vercel
- Netlify

**3. 修改API地址**：
编辑 `dist/config.json`：
```json
{
  "apiBaseUrl": "https://your-api-domain.com"
}
```

**4. Nginx 配置示例**：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

## 🔧 开发建议

### 添加新页面

**1. 创建页面组件**：
```bash
touch src/views/MyNewPage.vue
```

**2. 添加路由**：
```javascript
// src/router/index.js
import MyNewPage from '../views/MyNewPage.vue'

const routes = [
  {
    path: '/',
    component: Home,
    children: [
      {
        path: 'my-new-page',
        name: 'MyNewPage',
        component: MyNewPage
      }
    ]
  }
]
```

**3. 添加菜单**：
```vue
<!-- src/views/Home.vue -->
<el-menu-item index="/my-new-page">
  <el-icon><document /></el-icon>
  <span>我的新页面</span>
</el-menu-item>
```

---

### 自定义主题

修改 Element Plus 主题色：

**方法1：CSS变量**（推荐）
```css
/* src/main.js 或 App.vue */
:root {
  --el-color-primary: #409EFF; /* 主题色 */
}
```

**方法2：导入自定义主题**
```javascript
// main.js
import 'element-plus/theme-chalk/index.css'
```

---

## 📝 开发规范

### 组件命名

- 页面组件：PascalCase（如 `VersionManager.vue`）
- 通用组件：PascalCase（如 `VersionForm.vue`）

### API调用

统一使用 `async/await`：
```javascript
const fetchData = async () => {
  loading.value = true
  try {
    const config = await getConfig()
    const res = await axios.get(`${config.apiBaseUrl}/api/...`)
    // 处理数据
  } catch (error) {
    ElMessage.error('请求失败')
  } finally {
    loading.value = false
  }
}
```

### 错误处理

使用 Element Plus 的消息提示：
```javascript
import { ElMessage } from 'element-plus'

// 成功
ElMessage.success('操作成功')

// 错误
ElMessage.error('操作失败')

// 警告
ElMessage.warning('请注意')
```

---

## 📄 License

ISC

---

## 👨‍💻 作者

lemonguo121

---

## 📮 相关链接

- 后端 API 服务: [version_server](../version_server)
- Element Plus 文档: https://element-plus.org/
- Vue 3 文档: https://cn.vuejs.org/
- Vite 文档: https://cn.vitejs.dev/
